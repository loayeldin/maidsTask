import { ResizeNavDirective } from './resize-nav.directive';

describe('ResizeNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeNavDirective();
    expect(directive).toBeTruthy();
  });
});
